import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "FAQ | Katalog AI — Shopify Catalog Optimization",
  description: "Get answers about Katalog AI: how AI catalog optimization works, supported platforms, pricing plans, Shopify integration, and store connection process.",
};

export default function FaqLayout({ children }: { children: React.ReactNode }) {
  return children;
}
