import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "FAQ | Katalog AI — Your Shopify Catalog, Optimized by AI.",
  description: "Get answers about Katalog AI: how it works, pricing plans, Shopify integration, and store connection process.",
};

export default function FaqLayout({ children }: { children: React.ReactNode }) {
  return children;
}
